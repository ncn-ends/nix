" autocmd Filetype nix setlocal tabstop=2
" autocmd Filetype nix setlocal shiftwidth=2
" autocmd Filetype nix setlocal expandtab

" temporarily while working with nix files, prev lines don't work
set tabstop=2
set shiftwidth=2
set expandtab